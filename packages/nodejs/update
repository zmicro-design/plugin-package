#!/bin/bash

help() {
  echo "Usage:"
  echo "  zpackage update nodejs"
  echo ""
}

core() {
  if [ "$1" = "-h" ] || [ "$1" = "--help" ]; then
    help
    exit 0
  fi
  
  config_profile

  install_nvm

  install_nodejs

  update_packages
}

run() {
  core $@
}

run $@
